$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
command: python -c "print('hello world')"
environment:
  docker:
    image: python
compute:
  target: azureml:automl-compute
experiment_name: cli-examples
